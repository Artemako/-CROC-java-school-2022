package src.ru.croc.lessons.lesson3;

public class Example {
    public static void main(String[] args) {
        /*
        class Date{
            static int numInstances;
            // конструктор класса Date
            Date() {
                numInstances++;
            }
        }
         */
        /*
        class Date {
            static Date atEndOfYear(int year) {
                return new Date(year, DECEMBER, 31);
            }
        }
        d = Date.atEndOfYear();
        */
        /*
        Инкапсуляция - ограничение доступа; состояние объекта (private, default, protected, public)
        Наследование - иерархия (ограничение дублирования кода)
        Полиморфизм - использовать уникальное поведение с общим предком
        Абстрагирование - выделение значимых характеристик (abstract, interface, implements)
         */
        // @override
        /*
        max (arr) - max(int[] arr)
        max(arr), max(1,2,3) - max(int... arr){ arr[0], arr[1]}
         */
        // SOLID
        /*
        public enum Month{
            JANUARY,
            FEBRUARY
        }
        */
        //p1.equals(p2)

    }
}
